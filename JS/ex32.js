
const length = 10;
const width = 4;
const perimeter = 2 * (length + width);
const area = width * length;

console.log(`The rectangle perimeter is ${perimeter}`)
console.log(`The rectangle area is ${area}`)
console.log(`The height is ${length} and the width is ${width}`);